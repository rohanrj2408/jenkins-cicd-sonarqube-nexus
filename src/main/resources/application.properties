spring.application.name=ci-demo
server.port=8080

# Expose a simple health endpoint via the app's own controller; Actuator is not
# pulled in to keep the dependency surface small for this pipeline demo.
