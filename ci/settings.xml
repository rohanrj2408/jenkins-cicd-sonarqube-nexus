<?xml version="1.0" encoding="UTF-8"?>
<!--
  Settings used by the Jenkins "Publish to Nexus" stage:  mvn deploy -s ci/settings.xml

  The credentials are read from environment variables, which Jenkins injects via
  withCredentials() at deploy time. Nothing secret lives in this file, so it's
  safe to commit. The server <id>s match <distributionManagement> in pom.xml.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
  <servers>
    <server>
      <id>nexus-releases</id>
      <username>${env.NEXUS_USERNAME}</username>
      <password>${env.NEXUS_PASSWORD}</password>
    </server>
    <server>
      <id>nexus-snapshots</id>
      <username>${env.NEXUS_USERNAME}</username>
      <password>${env.NEXUS_PASSWORD}</password>
    </server>
  </servers>
</settings>
